---
import roadhog from "../assets/images/roadhog_normal.gif"


enum SoundType {
    Hook,
    Roadhog,
    Other
}
---

<div id="roadhog" class="unselectable">
    <div id="hogDialog" class="undraggable"></div>
    <div class="roadhogEntitySection" style="position: relative">    
        <!-- <span id="audioButton" class="material-symbols-outlined" data-toggle="modal" data-target="#exampleModalLong"></span> -->
        <img draggable="false" id="roadhogImage" src={roadhog.src}>
    </div>
</div>



<script>

    const roadhogImage = document.getElementById("roadhogImage")


    roadhogImage?.addEventListener("click", playRandomAudio)

    const roadhogAudio = new Audio()




    function playRandomAudio()
    {
        const audioType = Math.floor(Math.random() * 10)
        roadhogAudio.src = `/audio/shittalking/audio${audioType}.mp3`
        roadhogAudio.play();
        jump()
    }

    function jump()
    {
        if(roadhogImage == undefined) return;
        roadhogImage.classList.remove("hogJumping");   
        roadhogImage.offsetLeft;
        roadhogImage.classList.add("hogJumping");
    }


</script>


<style>

    .undraggable
    {
        -moz-window-dragging: no-drag;
        -webkit-user-drag: none;
        pointer-events: none;
        -moz-user-select: -moz-none;
        -khtml-user-select: none;
        -webkit-user-select: none;
        -o-user-select: none;
        user-select: none;
    }


    .unselectable
    {
        -moz-user-select: -moz-none;
        -khtml-user-select: none;
        -webkit-user-select: none;
        -o-user-select: none;
        user-select: none;

        -moz-window-dragging: no-drag;
        -webkit-user-drag: none;
    }

    #roadhog
    {
        display: flex;
        flex-direction: column;
        width: 20%;
        position: fixed;
        bottom: 0px;
        left: 0px;
        min-width: 130px;
        max-width: 220px;
        z-index: 500;
    }

    #roadhogImage
    {
        width: 100%;
        margin: 0px auto;
        display: block;
        cursor: pointer;
        filter: drop-shadow(-10px 2px 14px #353535);
    }


    .hogJumping
    {
        animation: hogJump .56s ease-in-out forwards;
    }

    #hogDialog
    {
        word-wrap: break-word;
        text-align: center;
        padding: 10px;
        border-style: groove;
        border-radius: 5px;
        border-width: 2px;
        border-color: black;
        background-color: white;
        opacity: 0;
        z-index: 1;
        transition: all 0.2s ease-in;
    }

    .hook
    {
        position: absolute;
        display: flex;
        flex-flow: column;

        --W: 20%;
        width: var(--W);
        position: absolute;
        transform-origin: bottom center;
        bottom: 50%;
        left: calc(50% - var(--W) / 2);
        z-index: -1;
        transform: rotate(var(--angle));
    }

    .chain
    {
        background-image: url("../media/hog/chain.png");
        width: 100%;
        background-size: contain;
        background-position-y: top;
        background-repeat: repeat-y;
        animation: throwHook 0.4s 2 cubic-bezier(.08,1.08,.83,.99) alternate;
    }

    .hookedItem
    {
        position: absolute;
        bottom: unset;
        right: unset;
        
    }

    @keyframes r
    {
        from
        {
            transform: rotate(0deg);
        }
        to
        {
            transform: rotate(360deg);
        }
    }

    #audioButton
    {
        position: absolute;
        z-index: 1;
        color: white;
        background-color: #353535;
        border-radius: 10px;
        padding: 6px;
        margin-top: 5px;
    }
    #audioButton::after
    {
        content: "volume_off";
    }
    #audioButton.on::after
    {
        content: "volume_up";
    }


    @keyframes throwHook {
        from{
            height: 0px;
        }
        to
        {
            height: var(--longness);
        }
    }

    @keyframes hogJump {
    
        20%,
        60% {
            transform: translateY(-10px);
        }
        40%,
        80% {
            transform: translateY(0px)
        }
    }
</style>